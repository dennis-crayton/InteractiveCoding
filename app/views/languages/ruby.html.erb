<style>
  .ruby-content {
    max-width: 900px;
    margin: 0 auto;
    padding: 20px;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    line-height: 1.6;
  }
  
  .ruby-content h1 {
    color: #CC342D;
    border-bottom: 3px solid #CC342D;
    padding-bottom: 10px;
  }
  
  .ruby-content h2 {
    color: #CC342D;
    margin-top: 40px;
  }
  
  .ruby-content pre {
    background: #595959ff;
    border-left: 4px solid #CC342D;
    padding: 15px;
    overflow-x: auto;
    font-family: 'Courier New', monospace;
    font-size: 14px;
    color: white;
  }

  /* FIXED EXERCISE PROMPTS — DARK MODE */
  .exercise-prompt {
    background: #1b1b1b; 
    border-left: 4px solid #CC342D;
    padding: 15px;
    margin: 20px 0;
    color: #ffffff !important;
  }
  
  .code-console {
    background: #1e1e1e;
    border-radius: 8px;
    padding: 20px;
    margin: 30px 0;
  }
  
  .code-console h3 {
    color: #fff;
    margin-top: 0;
  }
  
  .code-console textarea {
    width: 100%;
    min-height: 150px;
    background: #2d2d2d;
    color: #d4d4d4;
    border: 1px solid #444;
    border-radius: 4px;
    padding: 10px;
    font-family: 'Courier New', monospace;
    font-size: 14px;
  }
  
  .code-console button {
    background: #CC342D;
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 4px;
    cursor: pointer;
    font-size: 16px;
    margin-top: 10px;
  }
  
  .code-console button:hover {
    background: #a02822;
  }
  
  .output-box {
    background: #2d2d2d;
    color: #d4d4d4;
    border: 1px solid #444;
    border-radius: 4px;
    padding: 15px;
    margin-top: 15px;
    min-height: 100px;
    font-family: 'Courier New', monospace;
    white-space: pre-wrap;
  }

  /* FIX #2 — WHAT’S NEXT SECTION FIXED */
  .whats-next-box {
    margin-top: 60px;
    padding: 20px;
    background: #1e1e1e;
    border-radius: 8px;
    color: white;
  }

  .whats-next-box ul li {
    color: white;
  }
</style>

<div class="ruby-content">
  <h1>Ruby</h1>

  <section>
    <h2>Introduction</h2>
    <p>
      Ruby is a dynamic, object-oriented programming language that emphasizes simplicity and productivity. 
      Created by Yukihiro Matsumoto in the mid-1990s, Ruby is designed to be easy to read and write, 
      making it perfect for beginners while still being powerful enough for complex applications.
    </p>
    <p>
      In this guide, you'll learn the fundamentals of Ruby through examples and interactive exercises.
    </p>
  </section>

  <section>
    <h2>Basic Data Types</h2>
    <p>
      Ruby supports several fundamental data types. Understanding these is crucial for writing Ruby programs.
      Let's explore numbers, strings, booleans, arrays, and hashes.
    </p>
    
    <pre><code># Numbers
a = 1                  # integer
b = 2.5                # float
c = Complex(3, 6)      # complex

# Strings
d = 'A'
e = "Hello"            # double quotes allow interpolation
f = 'There\'s'         # single quotes require escaping

# Boolean
j = false              # only false and nil are false values

# Hash (key-value pairs)
hash = { "red" => "stop", "yellow" => "slow", "green" => "go" }

# Array (ordered collection)
mondays = [1, 8, 15, 22, 29]

# Output
puts "#{a}"
puts "#{b}"
puts "#{c}"
puts "#{d}"
puts "#{e}"
puts "#{f}"

hash.each do |key, value|
  print "if the sign is ", key, " it means ", value, "\n"
end

mondays.each do |i|
  puts i
end</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 1: Working with Data Types</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Create a variable called <code>name</code> with your name as a string, 
      a variable called <code>age</code> with your age as an integer, and print both using string interpolation.
    </div>
    <form class="ruby-console-form" data-exercise="1">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="1">Output will appear here...</div>
  </div>

  <section>
    <h2>Variables and Symbols</h2>
    <p>
      Ruby variables don't require type declarations. Ruby also has symbols, which are immutable identifiers 
      that are more memory-efficient when used as keys.
    </p>
    
    <pre><code># Numbers
int = 21
float = 4.01
rational = Rational(2, 3)
complex = Complex(1, 2)

# Strings
string = "Hello, world!"

# Symbols
sym = :Pao

# Booleans
bool = true

# Nil
likenull = nil

# Arrays
array = [1, 2, 3, "four", :five]

# Hashes
h = { name: "Colocho Famoso", age: 20 }

puts int
puts float
puts string
puts sym
puts bool
p nil
puts array
puts h</code></pre>
  </section>

  <section>
    <h2>Conditionals</h2>
    <p>Ruby uses <code>if</code>, <code>elsif</code>, and <code>else</code> for control flow.</p>

    <pre><code># Basic if-else
a = 1
b = 2
c = 3
d = 4
e = 5
f = 6

if d > a
  puts 'd > a'
end

if e < b
  puts 'e < b'
else
  puts 'e > b'
end

# elsif example
if f < c
  puts 'f < c'
elsif not (e >= b) && (d >= b)
  puts 'e <= b && d >= b'
else
  puts 'e <= c && d <= b'
  if (a <= f) || (d <= b)
    puts 'a <= f || d <= b'
  end
end

# Ternary operator
result = a > b ? 'a is bigger' : 'b is bigger'
puts result</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 2: Conditionals Practice</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Write a program that checks if a number is positive, negative, or zero.
    </div>
    <form class="ruby-console-form" data-exercise="2">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="2">Output will appear here...</div>
  </div>

  <section>
    <h2>Loops</h2>

    <pre><code># While loop
x = 5
while x >= 1
  puts x
  x -= 1
end

# For loop
arr = [1,2,3,4,5]
for i in arr do
  puts i
end

# Loop do
x = 1
loop do
  puts x
  x *= 2
  break if x >= 10
end</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 3: Loop Practice</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Print even numbers from 1–10 using a loop.
    </div>
    <form class="ruby-console-form" data-exercise="3">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="3">Output will appear here...</div>
  </div>

  <section>
    <h2>Methods</h2>

    <pre><code># Simple method
def hi
  puts "hello there"
end

# With parameter
def greet(name)
  puts "hello #{name}"
end

# Implicit return
def double_it(i)
  i * 2
end

# Explicit return
def half_it(i)
  r = i / 2.0
  return r
end</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 4: Methods Practice</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Create a <code>calculate_area</code> method.
    </div>
    <form class="ruby-console-form" data-exercise="4">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="4">Output will appear here...</div>
  </div>

  <section>
    <h2>Arrays and Iteration</h2>

    <pre><code># Arrays
numbers = [1,2,3,4,5]
mixed = [1,"hello",:symbol,true]

numbers.push(6)
numbers.unshift(0)
numbers.pop
numbers.shift

numbers.each do |num|
  puts num * 2
end</code></pre>
  </section>

  <section>
    <h2>Hashes</h2>

    <pre><code># Creating hashes
person = { "name" => "Alice", "age" => 30 }
student = { name: "Bob", age: 25 }

puts student[:name]
puts student[:age]

student[:major] = "CS"
student[:age] = 26

student.each do |key,value|
  puts "#{key}: #{value}"
end</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 5: Arrays and Hashes</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Create an array of hashes describing people.
    </div>
    <form class="ruby-console-form" data-exercise="5">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="5">Output will appear here...</div>
  </div>

  <section>
    <h2>Modules and Classes</h2>

    <pre><code># Module with methods
module AddOrSub
  def addition(x,y)
    x + y
  end

  def subtraction(x,y)
    x - y
  end
end

module Modulus
  def mod(x,y)
    x % y
  end
end

class Student
  include Modulus
  extend AddOrSub
  
  attr_accessor :name, :age

  def initialize(name,age)
    @name = name
    @age = age
  end
end

puts Student.addition(5,5)
puts Student.subtraction(10,5)

student = Student.new("Alice",20)
puts student.mod(19,4)</code></pre>
  </section>

  <div class="code-console">
    <h3>Exercise 6: Final Challenge</h3>
    <div class="exercise-prompt">
      <strong>Challenge:</strong> Build a simple calculator with methods.
    </div>
    <form class="ruby-console-form" data-exercise="6">
      <textarea name="code" placeholder="Write your Ruby code here..."></textarea>
      <input type="hidden" name="language" value="ruby">
      <button type="submit">Run Code</button>
    </form>
    <div class="output-box" data-output="6">Output will appear here...</div>
  </div>

  <!-- FIXED WHAT’S NEXT SECTION -->
  <section class="whats-next-box">
    <h2>What's Next?</h2>
    <p>
      You've learned the fundamentals of Ruby! From here, you can explore:
    </p>
    <ul>
      <li><strong>File I/O:</strong> Reading and writing files</li>
      <li><strong>Error Handling:</strong> Using begin/rescue/end blocks</li>
      <li><strong>Ruby Gems:</strong> Using external libraries</li>
      <li><strong>Rails Framework:</strong> Building web applications</li>
    </ul>
    <p>
      Keep practicing with the exercises above, and try modifying the examples to experiment with Ruby!
    </p>
  </section>

</div>

<script>
  document.querySelectorAll('.ruby-console-form').forEach(form => {
    form.addEventListener('submit', async function(e) {
      e.preventDefault();
      
      const exerciseNum = this.dataset.exercise;
      const outputBox = document.querySelector(`[data-output="${exerciseNum}"]`);
      const code = this.querySelector('textarea[name="code"]').value;
      const language = this.querySelector('input[name="language"]').value;
      const button = this.querySelector('button[type="submit"]');
      
      if (!code.trim()) {
        outputBox.textContent = 'Error: Please enter some code first!';
        outputBox.style.color = '#ff6b6b';
        return;
      }
      
      outputBox.textContent = '⏳ Running code...';
      outputBox.style.color = '#ffd43b';
      button.disabled = true;
      button.textContent = 'Running...';
      
      try {
        const formData = new FormData();
        formData.append('code', code);
        formData.append('language', language);
        
        const response = await fetch('/code/run', {
          method: 'POST',
          body: formData
        });
        
        if (!response.ok) throw new Error(`Server error: ${response.status}`);
        
        const result = await response.json();
        
        if (result.success) {
          outputBox.style.color = '#51cf66';
          outputBox.textContent = '✓ Output:\n' + result.output;
        } else {
          outputBox.style.color = '#ff6b6b';
          outputBox.textContent = '✗ ' + result.output;
        }
      } catch (error) {
        outputBox.style.color = '#ff6b6b';
        outputBox.textContent = '✗ Network Error: ' + error.message;
      } finally {
        button.disabled = false;
        button.textContent = 'Run Code';
      }
    });
  });
</script>
