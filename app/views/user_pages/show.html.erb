<style>
  body {
    background: #f5f5f5;
    min-height: 100vh;
    padding: 40px 20px;
  }
  
  .container {
    max-width: 1000px;
    margin: 0 auto;
    background: white;
    border-radius: 20px;
    padding: 40px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
  }
  
  .back-link {
    display: inline-block;
    margin-bottom: 20px;
    color: #667eea;
    text-decoration: none;
    font-weight: 600;
  }
  
  .header {
    border-bottom: 3px solid #667eea;
    padding-bottom: 20px;
    margin-bottom: 30px;
  }
  
  .header h1 {
    color: #333;
    margin-bottom: 15px;
  }
  
  .meta {
    display: flex;
    gap: 20px;
    flex-wrap: wrap;
    color: #666;
    font-size: 14px;
  }
  
  .meta-item {
    display: flex;
    align-items: center;
    gap: 5px;
  }
  
  .language-badge {
    display: inline-block;
    padding: 5px 15px;
    background: #667eea;
    color: white;
    border-radius: 20px;
    font-weight: 600;
    text-transform: uppercase;
    font-size: 12px;
  }
  
  .description {
    background: #f8f9fa;
    padding: 20px;
    border-left: 4px solid #667eea;
    margin-bottom: 30px;
    font-size: 16px;
    line-height: 1.6;
  }
  
  .actions {
    margin-bottom: 30px;
    display: flex;
    gap: 15px;
  }
  
  .btn {
    padding: 12px 24px;
    border-radius: 8px;
    text-decoration: none;
    font-weight: 600;
    transition: opacity 0.2s;
  }
  
  .btn-download {
    background: #28a745;
    color: white;
  }
  
  .btn:hover {
    opacity: 0.9;
  }
  
  .content {
    line-height: 1.8;
  }
  
  .content h2 {
    color: #333;
    margin-top: 30px;
    margin-bottom: 15px;
    border-left: 4px solid #667eea;
    padding-left: 15px;
  }
  
  .content h3 {
    color: #555;
    margin-top: 25px;
    margin-bottom: 12px;
  }
  
  .content p {
    margin-bottom: 15px;
    color: #444;
  }
  
  .content pre {
    background: #2d2d2d;
    color: #f8f8f2;
    padding: 20px;
    border-radius: 8px;
    overflow-x: auto;
    margin: 20px 0;
  }
  
  .content code {
    font-family: 'Courier New', monospace;
  }
  
  .content ul, .content ol {
    margin-left: 25px;
    margin-bottom: 15px;
  }
  
  .content li {
    margin-bottom: 8px;
  }
</style>

<div class="container">
  <%= link_to "← Back to Browse", user_pages_path, class: "back-link" %>
  
  <div class="header">
    <h1><%= @user_page.title %></h1>
    <div class="meta">
      <div class="meta-item">
        <span class="language-badge"><%= @user_page.language %></span>
      </div>
      <div class="meta-item">
        <strong>Author:</strong> <%= @user_page.author %>
      </div>
      <div class="meta-item">
        <strong>Downloads:</strong> <%= @user_page.downloads %>
      </div>
      <div class="meta-item">
        <strong>Created:</strong> <%= @user_page.created_at.strftime("%B %d, %Y") %>
      </div>
    </div>
  </div>
  
  <div class="description">
    <%= @user_page.description %>
  </div>
  
  <div class="actions">
    <%= link_to "⬇ Download Tutorial", user_pages_download_path(@user_page), class: "btn btn-download" %>
  </div>
  
  <div class="content">
    <%= raw @user_page.content %>
  </div>
</div>