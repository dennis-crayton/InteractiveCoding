<div class="page-card" data-id="<%= page.id %>">
  <div class="page-title"><%= page.title %></div>
  <div class="page-meta">
    <strong><%= page.language.capitalize %></strong> • by <%= page.author %>
  </div>
  <div class="page-description"><%= page.description %></div>
  <a href="<%= user_page_path(page) %>" class="btn btn-view">View</a>
  <a href="<%= user_pages_download_path(page) %>" class="btn btn-download">Download</a>
  <button class="btn btn-delete" style="background:#e74c3c; color:white;">Delete</button>
</div>
