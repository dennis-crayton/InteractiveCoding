<div class="navbar">
  <%= link_to "Home", root_path, class: ("active" if current_page?(root_path)) %>

  <div class="dropdown">
    <button class="dropbtn">Concepts <i class="fa fa-caret-down"></i></button>
    <div class="dropdown-content">
      <%= link_to "Ruby Concepts", "/languages/ruby", class: ("active" if current_page?("/languages/ruby")) %>
      <%= link_to "Python Concepts", "/languages/python", class: ("active" if current_page?("/languages/python")) %>
      <%= link_to "Java Concepts", "/languages/java", class: ("active" if current_page?("/languages/java")) %>
    </div>
  </div>

  <div class="dropdown">
    <button class="dropbtn">Sandbox <i class="fa fa-caret-down"></i></button>
    <div class="dropdown-content">
      <%= link_to "Ruby", languages_ruby_sandbox_path, class: ("active" if current_page?(languages_ruby_sandbox_path)) %>
      <%= link_to "Python", languages_python_sandbox_path, class: ("active" if current_page?(languages_python_sandbox_path)) %>
      <%= link_to "Java", languages_java_sandbox_path, class: ("active" if current_page?(languages_java_sandbox_path)) %>
    </div>
  </div>

  <!-- Optional: Dark/light theme toggle -->
  <button id="themeToggle" class="theme-toggle">🌓</button>
</div>
